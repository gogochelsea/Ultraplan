# 05. 검증된 헬퍼 함수

새로 작성하는 HWP 자동화 스크립트는 **무조건 이 헬퍼 9개를 통째로 복사**해서 시작한다. 함정이 모두 회피된 검증된 코드.

---

## 그대로 복붙할 코드 블록

```python
# -*- coding: utf-8 -*-
import os
import win32com.client as win32

# ── 경로 ──
BASE = r"C:\Users\<USER>\Desktop\<폴더>"
OUT_HWP = os.path.join(BASE, "결과물.hwp")
OUT_PDF = os.path.join(BASE, "결과물.pdf")

# ── COM 초기화 ──
hwp = win32.gencache.EnsureDispatch("HWPFrame.HwpObject")
try:
    hwp.RegisterModule("FilePathCheckDLL", "FilePathCheckerModule")
except Exception:
    pass
hwp.XHwpWindows.Item(0).Visible = False
hwp.SetMessageBoxMode(0x2FF1)
hwp.HAction.Run("FileNew")


# ─────────────────────────────────────────────────────────
# 1. set_char_shape — 매 write 직전에 호출. 7개 언어 서브셋
#    전부에 FontType=1, Ratio=Size=100 강제 (안 하면 글자 안 보임)
# ─────────────────────────────────────────────────────────
def set_char_shape(size_pt=12, bold=False, italic=False, color=0, face="함초롬바탕"):
    hwp.HAction.GetDefault("CharShape", hwp.HParameterSet.HCharShape.HSet)
    cs = hwp.HParameterSet.HCharShape
    cs.Height = int(size_pt * 100)
    cs.Bold = 1 if bold else 0
    cs.Italic = 1 if italic else 0
    cs.TextColor = color
    for ln in ["Hangul", "Latin", "Hanja", "Japanese", "Other", "Symbol", "User"]:
        setattr(cs, f"FaceName{ln}", face)
        setattr(cs, f"FontType{ln}", 1)   # ★ TTF
        setattr(cs, f"Offset{ln}", 0)
        setattr(cs, f"Ratio{ln}", 100)
        setattr(cs, f"Spacing{ln}", 0)
        setattr(cs, f"Size{ln}", 100)
    hwp.HAction.Execute("CharShape", cs.HSet)


# ─────────────────────────────────────────────────────────
# 2. set_para_shape — 정렬, 줄간격, 단락 앞뒤 여백
# ─────────────────────────────────────────────────────────
def set_para_shape(align="left", line_space=160, space_before=0, space_after=0):
    hwp.HAction.GetDefault("ParagraphShape", hwp.HParameterSet.HParaShape.HSet)
    ps = hwp.HParameterSet.HParaShape
    align_map = {"left": 0, "center": 1, "right": 2, "justify": 3, "distribute": 4}
    ps.AlignType = align_map.get(align, 0)
    ps.LineSpacing = line_space
    ps.LineSpacingType = 0
    ps.PrevSpacing = space_before
    ps.NextSpacing = space_after
    hwp.HAction.Execute("ParagraphShape", ps.HSet)


# ─────────────────────────────────────────────────────────
# 3. insert_text — 현재 위치에 텍스트 한 덩어리 삽입
# ─────────────────────────────────────────────────────────
def insert_text(text):
    if not text:
        return
    hwp.HAction.GetDefault("InsertText", hwp.HParameterSet.HInsertText.HSet)
    hwp.HParameterSet.HInsertText.Text = text
    hwp.HAction.Execute("InsertText", hwp.HParameterSet.HInsertText.HSet)


# ─────────────────────────────────────────────────────────
# 4. br — 단락 나눔 (Enter)
# ─────────────────────────────────────────────────────────
def br():
    hwp.HAction.Run("BreakPara")


# ─────────────────────────────────────────────────────────
# 5. page_break — 페이지 나눔
# ─────────────────────────────────────────────────────────
def page_break():
    hwp.HAction.Run("BreakPage")


# ─────────────────────────────────────────────────────────
# 6. write_line — 한 줄 쓰고 엔터 (제목·짧은 텍스트용)
# ─────────────────────────────────────────────────────────
def write_line(text, size=12, bold=False, align="left", color=0):
    set_char_shape(size_pt=size, bold=bold, color=color)
    set_para_shape(align=align)
    insert_text(text)
    br()


# ─────────────────────────────────────────────────────────
# 7. write_para — 문단(자동 줄바꿈) 쓰고 엔터 (본문용)
# ─────────────────────────────────────────────────────────
def write_para(text, size=12, bold=False, align="justify"):
    set_char_shape(size_pt=size, bold=bold)
    set_para_shape(align=align)
    insert_text(text)
    br()


# ─────────────────────────────────────────────────────────
# 8. insert_picture — 이미지 삽입. PIL 비율 + 130mm 캡 +
#    HeadCtrl로 컨트롤 찾아 Width/Height 강제 (안 하면 너무 큼)
# ─────────────────────────────────────────────────────────
def insert_picture(path, width_mm=120, height_mm=None, caption=None):
    if not os.path.exists(path):
        print(f"[WARN] image not found: {path}")
        return

    try:
        from PIL import Image
        with Image.open(path) as im:
            iw, ih = im.size
        aspect = ih / iw
    except Exception:
        aspect = 0.7

    if height_mm is None:
        height_mm = width_mm * aspect
        if height_mm > 130:           # ★ 한 페이지 안에 들어가도록 캡
            height_mm = 130
            width_mm = height_mm / aspect

    set_para_shape(align="center")
    set_char_shape(size_pt=12)

    HWPMM = 283.464567
    w_unit = int(width_mm * HWPMM)
    h_unit = int(height_mm * HWPMM)

    try:
        hwp.InsertPicture(path, True, 2, False)
    except Exception:
        try:
            hwp.InsertPicture(path, True, 0, False)
        except Exception as e:
            print("InsertPicture err:", e)

    # ★ 마지막 gso 컨트롤 찾아 크기 강제
    try:
        last = hwp.HeadCtrl
        target = None
        while last:
            if last.CtrlID == "gso":
                target = last
            last = last.Next
        if target:
            prop = target.Properties
            prop.SetItem("Width",  w_unit)
            prop.SetItem("Height", h_unit)
            target.Properties = prop
    except Exception as e:
        print("Resize err:", e)

    br()

    if caption:
        set_char_shape(size_pt=11, bold=False)
        set_para_shape(align="center")
        insert_text(caption)
        br()

    set_para_shape(align="left")  # ★ 다음 본문 위해 정렬 복귀


# ─────────────────────────────────────────────────────────
# 9. make_table — 실제 HWP 표. 매 호출마다 CreateAction/CreateSet
#    (싱글턴 재사용 시 2번째부터 COM 오류). HeightType=0 자동확장.
#    표 빠져나오기는 SetPos → MoveLineEnd → BreakPara.
# ─────────────────────────────────────────────────────────
def make_table(headers, rows, col_widths_mm, body_align="center", row_h_mm=12):
    cols = len(headers)
    nrows = len(rows) + 1
    total_w = sum(col_widths_mm)

    set_para_shape(align="center")
    set_char_shape(size_pt=10)

    saved_pos = hwp.GetPos()  # ★ 표 만들기 전 부모 단락 위치

    # ★ 매 호출마다 새 액션·새 파라미터셋
    act = hwp.CreateAction("TableCreate")
    pset = act.CreateSet()
    act.GetDefault(pset)
    pset.SetItem("Rows", nrows)
    pset.SetItem("Cols", cols)
    pset.SetItem("WidthType", 2)
    pset.SetItem("HeightType", 0)
    pset.SetItem("WidthValue",  hwp.MiliToHwpUnit(total_w))
    pset.SetItem("HeightValue", hwp.MiliToHwpUnit(row_h_mm * nrows))
    arr_w = pset.CreateItemArray("ColWidth", cols)
    for i, w in enumerate(col_widths_mm):
        arr_w.SetItem(i, hwp.MiliToHwpUnit(w))
    arr_h = pset.CreateItemArray("RowHeight", nrows)
    for i in range(nrows):
        arr_h.SetItem(i, hwp.MiliToHwpUnit(row_h_mm))
    act.Execute(pset)

    # 셀 채우기
    all_data = [headers] + rows
    for r, row in enumerate(all_data):
        for c, val in enumerate(row):
            set_char_shape(size_pt=10, bold=(r == 0))
            set_para_shape(align=("center" if r == 0 else body_align), line_space=150)
            insert_text(str(val))
            if not (r == nrows - 1 and c == cols - 1):
                hwp.HAction.Run("TableRightCell")

    # ★ 표 빠져나오기
    hwp.SetPos(saved_pos[0], saved_pos[1], saved_pos[2])
    hwp.HAction.Run("MoveLineEnd")
    set_char_shape(size_pt=12, bold=False)
    set_para_shape(align="left")
    br()
```

---

## 최소 동작 예제 (helper 사용 데모)

위 코드 블록 다음에 그대로 이어 붙이면 됩니다.

```python
# ── 표지 ──
for _ in range(4):
    write_line("", size=12)
write_line("사 업 계 획 서", size=32, bold=True, align="center")
write_line("", size=12)
write_line("부제목 한 줄", size=20, bold=True, align="center")
for _ in range(8):
    write_line("", size=12)
write_line("작성자 이름", size=16, align="center")
page_break()

# ── 본문 1장 ──
write_line("1. 사업 개요", size=12, bold=True)
write_line("", size=12)
write_line("-사업 추진 배경", size=12, bold=True)
write_para(
    "본 사업의 추진 배경은 다음과 같다. "
    "외벽 단열재 시장의 규제 강화 흐름에 대응하기 위해 친환경 준불연 단열보드를 개발한다."
)

# ── 표 ──
write_line("<표 1> 시장 규모", size=11, bold=True, align="center")
make_table(
    headers=["구분", "정의", "규모"],
    rows=[
        ["TAM", "전체 시장",      "2.4조 원"],
        ["SAM", "유효 시장",      "9,100억 원"],
        ["SOM", "획득 가능 시장", "1,200억 원"],
    ],
    col_widths_mm=[25, 75, 50],
    body_align="left",
)

# ── 그림 ──
insert_picture(
    r"C:\path\to\image.png",
    width_mm=120,
    caption="<그림 1> 시험 장비 사진",
)

# ── 페이지 번호 - N - ──
hwp.HAction.GetDefault("PageNumPos", hwp.HParameterSet.HPageNumPos.HSet)
pn = hwp.HParameterSet.HPageNumPos
pn.Pos = 8
pn.NumberFormat = 0
pn.SideChar = 1
hwp.HAction.Execute("PageNumPos", pn.HSet)

# ── 저장 + PDF 변환 ──
hwp.SaveAs(OUT_HWP, "HWP", "")
print("HWP saved:", OUT_HWP)

hwp.HAction.GetDefault("FileSaveAsPdf", hwp.HParameterSet.HFileOpenSave.HSet)
hwp.HParameterSet.HFileOpenSave.filename = OUT_PDF
hwp.HParameterSet.HFileOpenSave.Format = "PDF"
hwp.HAction.Execute("FileSaveAsPdf", hwp.HParameterSet.HFileOpenSave.HSet)
print("PDF saved:", OUT_PDF)

print("Pages:", hwp.PageCount)
hwp.Quit()
```

---

## 의존성

- **Python 패키지**: `pywin32` (`pip install pywin32`), `Pillow` (`pip install pillow`)
- **시스템**: Windows + Hancom Office (한글) 설치
- **폰트**: 함초롬바탕 (Hancom Office 설치 시 함께 설치). 다른 폰트로 바꾸려면 `set_char_shape(face="맑은 고딕")` 식으로 인자 전달

---

## 다음 단계

- 함정 회피 원리가 궁금하면 → [01](./01-글자-렌더링.md), [02](./02-이미지-크기제어.md), [03](./03-표-안전생성.md)
- 처음으로 → [README.md](./README.md)
